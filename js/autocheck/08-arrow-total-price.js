/* выполни рефакторинг функции calculateTotalPrice() 
так, чтобы она была объявлена как стрелочная */

"use strict";

const calculateTotalPrice = (quantity, pricePerItem) => {
  return quantity * pricePerItem;
};
